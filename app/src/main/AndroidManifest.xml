<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InterLeague"
        tools:targetApi="31">

        <!-- Pantalla de carga como actividad principal -->
        <activity
            android:name=".PantallaDeCarga"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras actividades del proyecto -->
        <activity android:name=".EquipoActivity" android:exported="true"/>
        <activity android:name=".LigasActivity" android:exported="true"/>
        <activity android:name=".LoginActivity" android:exported="true"/>
        <activity android:name=".MainActivity" android:exported="true"/>
        <activity android:name=".MercadoActivity" android:exported="true"/>
        <activity android:name=".MyTeamActivity" android:exported="true"/>
        <activity android:name=".PantallaDeEspera" android:exported="true"/>
        <activity android:name=".PerfilUsuarioActivity" android:exported="true"/>
        <activity android:name=".ReestablecerContrasena" android:exported="true"/>
        <activity android:name=".Registrarse" android:exported="true"/>
        <activity android:name=".SettingsActivity" android:exported="true"/>
        <activity android:name=".CrearLigaActivity" />
        <activity android:name=".ListaJugadoresFirebaseActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" android:exported="true"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
